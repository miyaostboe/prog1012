# Fill in the blanks so the answers are all 2. Use help for unfamiliar functions
# if necessary.
?log10
log10(100)
sqrt(4)
ceiling(1.1)
?ceiling
# ceiling <- rounds up
# floor <- rounds down

# Find the functions for cosine and sine. What are the cosine and sine of 0?
cos(0)
sin(0)

# What do you think the following functions do? Just go off the name, don't look
# them up.
read.csv()
# able to read a document or decode
# files for storing spreadsheets
boxplot()
# plot points in a certain order
# creates box and whisker plot
startsWith()
# phrase and sequence that start with a certain character
# startsWith("Max", "M") <- TRUE
